Traceback (most recent call last):
  File "<stdin>", line 42, in <module>
  File "<stdin>", line 4, in rep
AssertionError: (0, 'URI, then range start. Truncate to 200. For an empty query every symbo')
[0m
> build · gpt-5.6-sol
[0m
I’ll read both specifications in full, then cross-check only implementation-breaking ambiguities or contradictions against the repository and prior review where needed.
[0m→ [0mRead SPEC.md
[0m→ [0mRead TASKS.md
[0m→ [0mRead docs/review-5-raw.txt
Several round-5 fixes are only partially or not actually reflected in the normative text. I’m checking whether any additional phase-specific conflict is independently implementation-breaking before limiting the report.
[0m→ [0mRead docs/gates.md
[0m→ [0mRead docs/review-log.md
[0m✱ [0mGrep "additionalProperties.*false|debug_adapter_schemas"[90m in /home/dig · 100 matches[0m
[0m✱ [0mGlob "**/debug_adapter_schemas/*.json"[90m in /home/dig · 0 matches[0m
**Phase 0: ready**

**Phase 1**

1. Quote: “An input that yields zero moves to the next.” / “`project_dir` setting ... Must contain `project.godot`, else error.”

Problem: An invalid configured `project_dir` can either fall through or fail immediately.

Exact fix text: “Zero results move to the next input except for an explicitly configured `project_dir`; if that path lacks `project.godot`, return `project_dir <p> has no project.godot` immediately.”

2. Quote: “Pending Zed requests: `id -> method`. Pending bridge-internal requests: string ids prefixed `godot-bridge/`.”

Problem: Zed may legally use the same string IDs, causing responses or cancellations to be misrouted.

Exact fix text: “Rewrite every Zed request ID sent to Godot to a unique bridge-owned ID and retain an original-ID mapping. Rewrite `$/cancelRequest` targets through that mapping and restore original IDs in responses to Zed. Allocate internal requests from the same collision-free sequence and consume only entries marked internal.”

**Phase 2**

3. Quote: “`run --scene current` goes through scene resolution.” / DAP specifies direct `.tscn` handling, but run does not.

Problem: `run --scene current` remains undefined for `.tscn` and non-`.gd` files.

Exact fix text: “For `run --scene current`, apply the same rule as DAP: a `.tscn` file resolves directly to its canonical `res://` path; any other file uses the reference-search scene resolution; no match exits 1 with `No scene uses <file>`.”

**Phase 3**

4. Quote: “Zed messages received from now on go into one FIFO” / “One task owns `open_docs` and serializes Zed events and watcher events.”

Problem: Watcher behavior during recovery remains undefined, allowing stale replay, lost changes, or writes to a dead connection.

Exact fix text: “During recovery, watcher events continue updating Bridge-owned `open_docs` state but emit no messages to Godot and schedule no symbol requests. Recovery replay uses that latest state. After `ready`, resume watcher output; Zed traffic remains governed by the recovery FIFO.”

**Phase 4**

5. Quote: State schema has `"owner_pid":N,"owner_start_ticks":N` / “`owner_pid` and `owner_start_ticks` are null only for a detached GUI state.”

Problem: The normative JSON schema excludes the detached state required later.

Exact fix text: “Change the state schema to `"owner_pid":null|N,"owner_start_ticks":null|N`; both fields must be null together only for a detached GUI state.”

6. Quote: “No socket or refused: launches `<godot> ...` detached” / later `lsp` reconnect requires recorded GUI state and ports.

Problem: A standalone GUI is not recorded or adoptable, so opening a script can start a second editor.

Exact fix text: “When no owner socket responds, `open-editor` must take the project lock before launching. Reuse a valid detached GUI state if present; otherwise remove stale state, allocate LSP and DAP ports, launch GUI with both port flags, wait for readiness, and write `mode: gui` state with null owner fields before releasing the lock. If the lock is held, retry owner-socket discovery and fail rather than launching another editor.”

NOT READY

[exited with code 0]
